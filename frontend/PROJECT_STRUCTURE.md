# Angular 21 Frontend - Project Structure Documentation

## 📁 Complete Directory Tree

```
frontend/
├── src/
│   ├── app/
│   │   ├── core/                              # Core functionality (singleton services)
│   │   │   ├── guards/
│   │   │   │   └── auth.guard.ts              # Route protection guard
│   │   │   ├── interceptors/
│   │   │   │   └── auth.interceptor.ts        # JWT token interceptor
│   │   │   ├── services/
│   │   │   │   ├── auth.service.ts            # Authentication service
│   │   │   │   └── weather.service.ts         # Weather API service
│   │   │   └── models/
│   │   │       ├── weather.model.ts           # Weather data interfaces
│   │   │       └── user.model.ts              # User profile interface
│   │   │
│   │   ├── features/                          # Feature modules
│   │   │   ├── auth/
│   │   │   │   ├── login/
│   │   │   │   │   ├── login.component.ts     # Login page component
│   │   │   │   │   ├── login.component.html   # Login template
│   │   │   │   │   └── login.component.scss   # Login styles
│   │   │   │   └── callback/
│   │   │   │       ├── callback.component.ts  # Auth0 callback handler
│   │   │   │       ├── callback.component.html
│   │   │   │       └── callback.component.scss
│   │   │   │
│   │   │   ├── dashboard/
│   │   │   │   ├── dashboard.component.ts     # Main dashboard component
│   │   │   │   ├── dashboard.component.html   # Dashboard template
│   │   │   │   └── dashboard.component.scss   # Dashboard styles
│   │   │   │
│   │   │   └── weather/
│   │   │       └── weather-detail/
│   │   │           ├── weather-detail.component.ts    # Weather detail component
│   │   │           ├── weather-detail.component.html  # Detail template
│   │   │           └── weather-detail.component.scss  # Detail styles
│   │   │
│   │   ├── shared/                            # Shared components and utilities
│   │   │   └── components/
│   │   │       ├── navbar/
│   │   │       │   ├── navbar.component.ts    # Navigation bar
│   │   │       │   ├── navbar.component.html
│   │   │       │   └── navbar.component.scss
│   │   │       ├── footer/
│   │   │       │   ├── footer.component.ts    # Footer component
│   │   │       │   ├── footer.component.html
│   │   │       │   └── footer.component.scss
│   │   │       ├── loading/
│   │   │       │   ├── loading.component.ts   # Loading spinner
│   │   │       │   ├── loading.component.html
│   │   │       │   └── loading.component.scss
│   │   │       └── error/
│   │   │           ├── error.component.ts     # Error display
│   │   │           ├── error.component.html
│   │   │           └── error.component.scss
│   │   │
│   │   ├── app.ts                             # Root component
│   │   ├── app.html                           # Root template
│   │   ├── app.scss                           # Root styles
│   │   ├── app.config.ts                      # App configuration & providers
│   │   ├── app.routes.ts                      # Route configuration
│   │   └── app.spec.ts                        # Root component tests
│   │
│   ├── environments/
│   │   ├── environment.ts                     # Development environment
│   │   └── environment.prod.ts                # Production environment
│   │
│   ├── styles.scss                            # Global styles + Tailwind
│   ├── index.html                             # HTML entry point
│   └── main.ts                                # Application bootstrap
│
├── public/
│   └── favicon.ico                            # App icon
│
├── .vscode/                                   # VS Code settings
├── angular.json                               # Angular CLI configuration
├── package.json                               # Dependencies
├── package-lock.json                          # Dependency lock
├── tsconfig.json                              # TypeScript config
├── tsconfig.app.json                          # App-specific TS config
├── tsconfig.spec.json                         # Test-specific TS config
├── tailwind.config.js                         # Tailwind CSS configuration
├── postcss.config.js                          # PostCSS configuration
├── README.md                                  # Setup documentation
└── .gitignore                                 # Git ignore rules
```

## 🏗️ Architecture Layers

```
┌─────────────────────────────────────────────────────────────┐
│                      Presentation Layer                      │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  Components (Smart & Presentational)                   │  │
│  │  • Login, Dashboard, Weather Detail                    │  │
│  │  • Navbar, Footer, Loading, Error                      │  │
│  └────────────────────────────────────────────────────────┘  │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ↓
┌─────────────────────────────────────────────────────────────┐
│                     Business Logic Layer                     │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  Services                                              │  │
│  │  • AuthService (Auth0 wrapper)                         │  │
│  │  • WeatherService (API calls)                          │  │
│  └────────────────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  Guards & Interceptors                                 │  │
│  │  • authGuard (Route protection)                        │  │
│  │  • authInterceptor (Token injection)                   │  │
│  └────────────────────────────────────────────────────────┘  │
└────────────────────────┬────────────────────────────────────┘
                         │
                         ↓
┌─────────────────────────────────────────────────────────────┐
│                       Data Layer                             │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  Models (TypeScript Interfaces)                        │  │
│  │  • WeatherResponse, ComfortIndexResponse              │  │
│  │  • User, City                                          │  │
│  └────────────────────────────────────────────────────────┘  │
│  ┌────────────────────────────────────────────────────────┐  │
│  │  HTTP Client (Angular HttpClient)                     │  │
│  │  • REST API communication                             │  │
│  │  • RxJS Observable streams                            │  │
│  └────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

## 🔄 Data Flow

### Authentication Flow

```
┌─────────────┐         ┌──────────────┐         ┌────────────┐
│   Browser   │────1───▶│ LoginComponent│────2───▶│   Auth0    │
│             │         │               │         │  Service   │
└─────────────┘         └──────────────┘         └────────────┘
       ▲                                                 │
       │                                                 3
       │                                                 │
       │                                                 ▼
       │                 ┌──────────────┐         ┌────────────┐
       └────────6────────│   Dashboard  │◀───5────│  Callback  │
                         │   Component  │         │ Component  │
                         └──────────────┘         └────────────┘
                                │                        ▲
                                └───────────4────────────┘

Flow:
1. User clicks "Sign In"
2. LoginComponent triggers Auth0 login
3. Auth0 authenticates and redirects with code
4. CallbackComponent handles the code
5. User authenticated, navigate to Dashboard
6. Dashboard loads with user session
```

### Weather Data Flow

```
┌──────────────┐         ┌──────────────┐         ┌────────────┐
│   Dashboard  │────1───▶│   Weather    │────2───▶│ HTTP Client│
│  Component   │         │   Service    │         │            │
└──────────────┘         └──────────────┘         └────────────┘
       ▲                        │                        │
       │                        │                        3
       │                        │                        │
       │                        ▼                        ▼
       │                 ┌──────────────┐         ┌────────────┐
       └────────5────────│ BehaviorSub- │◀───4────│  Backend   │
                         │   ject Cache │         │    API     │
                         └──────────────┘         └────────────┘

Flow:
1. Component requests weather data
2. Service checks cache, makes HTTP call
3. Request sent to backend with JWT token
4. Backend returns weather data
5. Data cached and emitted to component
```

### Route Guard Flow

```
┌──────────────┐         ┌──────────────┐         ┌────────────┐
│   Router     │────1───▶│  authGuard   │────2───▶│   Auth0    │
│              │         │              │         │  Service   │
└──────────────┘         └──────────────┘         └────────────┘
       │                        │                        │
       │                        │                        3
       │                        │                        │
       │                        ▼                        ▼
       │                 ┌──────────────┐         ┌────────────┐
       │                 │   isAuth$    │────4───▶│  Decision  │
       │                 │  Observable  │         │            │
       │                 └──────────────┘         └────────────┘
       │                                                 │
       │                     ┌──────────────────────────┤
       │                     │                          │
       │                     ▼                          ▼
       │              ┌──────────────┐          ┌──────────────┐
       └─────True─────│   Navigate   │          │  Redirect to │
                      │  to Route    │          │    Login     │
                      └──────────────┘          └──────────────┘
                                                        False

Flow:
1. User navigates to protected route
2. Guard intercepts navigation
3. Check authentication status
4. Make routing decision based on auth state
```

## 📦 Component Relationships

```
App (Root)
│
├── NavbarComponent (Always visible)
│   ├── Logo
│   ├── Navigation Links
│   ├── User Profile Dropdown
│   └── Mobile Menu
│
├── Router Outlet
│   │
│   ├── LoginComponent (Public route)
│   │   └── Auth0 Login Button
│   │
│   ├── CallbackComponent (Public route)
│   │   └── LoadingComponent
│   │
│   ├── DashboardComponent (Protected route)
│   │   ├── Search Bar
│   │   ├── Refresh Button
│   │   ├── Weather Cards (Multiple)
│   │   │   ├── City Info
│   │   │   ├── Temperature
│   │   │   ├── Weather Icon
│   │   │   └── Comfort Badge
│   │   ├── LoadingComponent (Conditional)
│   │   └── ErrorComponent (Conditional)
│   │
│   └── WeatherDetailComponent (Protected route)
│       ├── Back Button
│       ├── Refresh Button
│       ├── Weather Info Section
│       │   ├── Large Temperature
│       │   ├── Weather Icon
│       │   └── Feels Like
│       ├── Metrics Grid
│       │   ├── Humidity
│       │   ├── Wind Speed
│       │   ├── Pressure
│       │   ├── Cloudiness
│       │   └── Visibility
│       ├── Comfort Index Section
│       │   ├── Overall Score
│       │   ├── Temperature Impact Bar
│       │   ├── Humidity Impact Bar
│       │   └── Wind Impact Bar
│       ├── LoadingComponent (Conditional)
│       └── ErrorComponent (Conditional)
│
└── FooterComponent (Always visible)
    ├── Copyright
    └── Attribution
```

## 🎯 Dependency Injection Tree

```
AppConfig (Root)
│
├── Providers
│   ├── Router (provideRouter)
│   ├── HttpClient (provideHttpClient)
│   │   └── Interceptors
│   │       └── authInterceptor
│   └── Auth0 (provideAuth0)
│       ├── domain
│       ├── clientId
│       └── httpInterceptor config
│
└── Components (Injected Services)
    │
    ├── LoginComponent
    │   └── inject(AuthService)
    │
    ├── CallbackComponent
    │   ├── inject(AuthService)
    │   └── inject(Router)
    │
    ├── NavbarComponent
    │   └── inject(AuthService)
    │
    ├── DashboardComponent
    │   ├── inject(WeatherService)
    │   └── inject(Router)
    │
    └── WeatherDetailComponent
        ├── inject(WeatherService)
        ├── inject(ActivatedRoute)
        └── inject(Router)
```

## 🔐 Security Layers

```
Request Flow with Security

┌──────────────────────────────────────────────────────────┐
│  1. Component makes API call                             │
│     weatherService.getCities()                           │
└────────────────────┬─────────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────────┐
│  2. HTTP Interceptor Chain                               │
│     ┌──────────────────────────────────────────────┐     │
│     │  authInterceptor                             │     │
│     │  • Gets JWT token from Auth0                 │     │
│     │  • Adds Authorization header                 │     │
│     │  • Bearer <token>                            │     │
│     └──────────────────────────────────────────────┘     │
└────────────────────┬─────────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────────┐
│  3. HTTP Request                                         │
│     GET http://localhost:8080/api/v1/weather/cities      │
│     Headers:                                             │
│       Authorization: Bearer eyJhbGc...                   │
│       Content-Type: application/json                     │
└────────────────────┬─────────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────────┐
│  4. Backend Security Filter Chain                        │
│     • CORS validation                                    │
│     • JWT token validation                              │
│     • Audience verification                             │
│     • Issuer verification                               │
│     • Token expiration check                            │
└────────────────────┬─────────────────────────────────────┘
                     │
                     ▼
┌──────────────────────────────────────────────────────────┐
│  5. Response                                             │
│     • 200 OK: Data returned                             │
│     • 401 Unauthorized: Redirect to login               │
│     • 403 Forbidden: Access denied                      │
│     • 500 Error: Server error                           │
└──────────────────────────────────────────────────────────┘
```

## 📊 State Management

```
Application State

┌──────────────────────────────────────────────────────────┐
│  Auth0 SDK (Built-in state management)                  │
│  ┌────────────────────────────────────────────────┐     │
│  │  user$ (Observable)                            │     │
│  │  isAuthenticated$ (Observable)                 │     │
│  │  isLoading$ (Observable)                       │     │
│  │  getAccessTokenSilently$ (Observable)          │     │
│  └────────────────────────────────────────────────┘     │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│  WeatherService (Custom state management)                │
│  ┌────────────────────────────────────────────────┐     │
│  │  weatherCache$ (BehaviorSubject)               │     │
│  │  • Stores latest weather data                  │     │
│  │  • Emits to all subscribers                    │     │
│  │  • Cached for 5 minutes                        │     │
│  └────────────────────────────────────────────────┘     │
└──────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────┐
│  Component Local State                                   │
│  ┌────────────────────────────────────────────────┐     │
│  │  • isLoading (boolean)                         │     │
│  │  • error (string | null)                       │     │
│  │  • searchQuery (string)                        │     │
│  │  • weatherData (WeatherResponse[])             │     │
│  └────────────────────────────────────────────────┘     │
└──────────────────────────────────────────────────────────┘
```

## 🎨 Styling Architecture

```
Tailwind CSS Configuration

tailwind.config.js
│
├── Content Paths
│   └── "./src/**/*.{html,ts}"
│
└── Theme Extensions
    ├── colors
    │   ├── primary: #3B82F6
    │   ├── secondary: #10B981
    │   ├── danger: #EF4444
    │   ├── warning: #F59E0B
    │   ├── good: #10B981
    │   ├── moderate: #F59E0B
    │   └── poor: #EF4444
    │
    ├── spacing
    │   ├── 4: 1rem
    │   └── 8: 2rem
    │
    ├── borderRadius
    │   └── standard: 8px
    │
    └── boxShadow
        └── card: 0 4px 6px rgba(...)

styles.scss
│
├── @tailwind base
├── @tailwind components
├── @tailwind utilities
│
└── Global Styles
    ├── * (Reset & Box-sizing)
    ├── body (Font & Background)
    └── Transitions (All elements)
```

---

This structure follows Angular best practices and provides a solid foundation for a production-ready application.
